# Server-only — never prefixed with NEXT_PUBLIC_, so it stays out of the client bundle.
# All API calls happen from Server Components/Actions, which proxy the user's session
# token to this URL as a Bearer header.
API_BASE_URL="http://localhost:3001/api/v1"
